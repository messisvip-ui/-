# 🏫 教室一体机部署指南

## 📦 版本说明

**版本**: v2.0.0 - 单机多用户完整版  
**适用场景**: 教室一体机，学生轮流上讲台使用  
**最后更新**: 2026-03-16

---

## 🎯 功能特性

### 学生端
- ✅ 学生登录/切换
- ✅ 个人精灵展示
- ✅ 任务系统
- ✅ 星光礼赞（同学互赞）
- ✅ 个人主页
- ✅ 精灵森林 3D 场景

### 教师端
- ✅ 学生管理（添加/编辑/删除）
- ✅ 任务管理（发布/编辑/删除）
- ✅ 班级统计
- ✅ 数据查看

### 数据存储
- ✅ 本地存储（localStorage）
- ✅ 无需数据库
- ✅ 无需后端服务器
- ✅ 刷新页面数据不丢失

---

## 🚀 快速部署

### 第 1 步：安装 Node.js

**Windows**:
1. 访问 https://nodejs.org/
2. 下载 LTS 版本（推荐 18.x 或 20.x）
3. 双击安装，一路 Next

**Mac**:
```bash
brew install node
```

**Linux**:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 第 2 步：克隆项目

```bash
# 创建项目目录
mkdir -p ~/Projects
cd ~/Projects

# 克隆仓库
git clone https://github.com/messisvip-ui/-.git light-contract
cd light-contract
```

### 第 3 步：安装依赖

```bash
npm install
```

### 第 4 步：启动服务

```bash
npm run dev
```

启动成功后会显示：
```
VITE v5.4.21  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

---

## 📱 使用流程

### 教师首次使用

1. **访问教师端**: http://localhost:3001
2. **点击"教师登录"**
3. **添加学生**:
   - 点击"➕ 添加学生"
   - 输入学生姓名（如：李明）
   - 输入学号（如：001）
   - 点击"确认添加"
4. **重复添加**所有 45 个学生
5. **发布任务**:
   - 切换到"任务管理"标签
   - 点击"➕ 发布任务"
   - 填写任务信息
   - 选择发布给全班或指定学生

### 学生使用

1. **访问学生端**: http://localhost:3000
2. **选择自己的名字**（头像卡片）
3. **点击"进入精灵森境"**
4. **完成任务**:
   - 点击"星光委托"
   - 查看任务列表
   - 点击"完成任务"获得星光
5. **查看精灵**:
   - 点击"精灵森林"查看 3D 场景
   - 点击"个人主页"查看成长记录

### 切换学生

1. 点击导航栏右上角的学生名字
2. 选择"退出登录"
3. 回到登录页选择下一个学生

---

## 📊 数据结构

### 存储位置
浏览器 localStorage，键名：`light-contract-class-data`

### 数据格式
```javascript
{
  "name": "三年级 2 班",
  "teacherName": "王老师",
  "students": [
    {
      "id": "001",
      "name": "李明",
      "studentId": "001",
      "avatar": "👤",
      "createdAt": "2026-03-16T12:00:00.000Z"
    },
    ...
  ],
  "tasks": [
    {
      "id": "task-1",
      "title": "晨间守护者",
      "description": "按时到校",
      "type": "daily",
      "starlightReward": 15,
      "intimacyReward": 5,
      "assignedTo": "all",
      "completedBy": [
        {"studentId": "001", "completedAt": "..."}
      ]
    },
    ...
  ],
  "praises": [...]
}
```

### 学生个人数据
每个学生独立存储：`student-{学生 ID}`

```javascript
{
  "sprite": {
    "name": "小光",
    "element": "light",
    "form": "seedling",
    "starlight": 100,
    "intimacyLevel": 1
  },
  "tasks": [],
  "receivedPraises": [],
  "totalStarlight": 100,
  "achievements": [],
  "unlockedAreas": ["misty-valley"]
}
```

---

## 🔧 常用操作

### 查看全班数据

打开浏览器控制台（F12），输入：
```javascript
const data = JSON.parse(localStorage.getItem('light-contract-class-data'))
console.log('班级:', data.name)
console.log('学生数:', data.students.length)
console.log('任务数:', data.tasks.length)
```

### 导出班级数据

```javascript
const data = localStorage.getItem('light-contract-class-data')
const blob = new Blob([data], {type: 'application/json'})
const url = URL.createObjectURL(blob)
const a = document.createElement('a')
a.href = url
a.download = `班级数据-${new Date().toLocaleDateString()}.json`
a.click()
```

### 导入班级数据

```javascript
// 在控制台粘贴 JSON 数据
const newData = {...} // 粘贴 JSON
localStorage.setItem('light-contract-class-data', JSON.stringify(newData))
location.reload()
```

### 重置所有数据

⚠️ **警告：这会删除所有数据！**

```javascript
localStorage.clear()
location.reload()
```

---

## 📝 教学计划示例

### 第一周：初始化

**教师准备**:
1. 添加全班 45 个学生
2. 发布 3-5 个每日任务
3. 测试学生登录

**示例任务**:
- 晨间守护者（按时到校）- 15 星光
- 知识探索者（完成作业）- 20 星光
- 互助小天使（帮助同学）- 10 星光

### 第二周：激励

**活动**:
1. 公布星光排行榜
2. 表扬前 5 名
3. 鼓励后进学生

### 第三周：互动

**活动**:
1. 介绍"星光礼赞"功能
2. 每天可以赞赏 3 次同学
3. 培养班级友好氛围

### 第四周：总结

**活动**:
1. 统计月度数据
2. 颁发"星光之星"奖
3. 导出数据存档

---

## 🎯 最佳实践

### 学生管理
- ✅ 使用学号作为 ID（唯一且有序）
- ✅ 添加学生时设置头像（emoji）
- ✅ 定期更新学生信息

### 任务管理
- ✅ 每日任务保持 3-5 个
- ✅ 每周更新特殊任务
- ✅ 任务描述要具体明确

### 数据备份
- ✅ 每周五导出数据备份
- ✅ 学期结束导出存档
- ✅ 更换电脑前导出数据

---

## 🐛 常见问题

### Q: 学生忘记带学号怎么办？
A: 在登录页显示学生列表，让学生自己找名字点击即可。

### Q: 数据丢失了怎么办？
A: 如果没有备份，只能重新添加学生和任务。建议定期备份。

### Q: 可以多个学生同时使用吗？
A: 不行，这是单机单用户设计。学生需要轮流上讲台操作。

### Q: 如何在投影仪上展示？
A: 连接投影仪后，浏览器全屏（F11），让全班看到操作过程。

### Q: 可以打印星光排行榜吗？
A: 可以，在控制台输入：
```javascript
const data = JSON.parse(localStorage.getItem('light-contract-class-data'))
console.table(data.students.map(s => ({
  姓名：s.name,
  学号：s.studentId
})))
```

---

## 📞 技术支持

### 查看版本
```bash
cd ~/Projects/light-contract
git log --oneline -1
```

### 更新到最新版本
```bash
cd ~/Projects/light-contract
git pull origin main
npm install
```

### 查看日志
启动后终端会显示访问日志和错误信息。

---

## 🎉 开始使用

```bash
# 启动服务
cd ~/Projects/light-contract
npm run dev

# 访问
学生端：http://localhost:3000
教师端：http://localhost:3001
```

**祝你使用愉快！** ✨
